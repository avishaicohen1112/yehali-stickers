# סטטוס הפרויקט

> עודכן: 2026-08-20 (אחרי תיקון חיתוך האייקון — ר' `00-יומן-החלטות.md`)

## עובד

| רכיב | ודאות | הערה |
|---|---|---|
| המשחק עצמו (כל המכניקות: תנועה, דאש, 4 סוגי חברה, 5 שדרוגים, קומבו, גלים) | ✅ | הורץ ונצפה חזותית פעמים רבות + נבדק בבוט אוטומטי (15+ ריצות מלאות, 0 שגיאות). ראה `screenshots/2026-08-20-gameplay-verified.png` |
| ביצועים בעומס מקסימלי (13 חברה, 4 סטיקרים, 100 חלקיקים) | ✅ | נמדד: 3.8ms update+render לפריים (תקציב 60fps = 16.7ms) |
| סמן הזיהוי (חץ+טבעת) מעל יהלי בערימת חברה | ✅ | תוקן אחרי שנצפה בפועל שיהלי נעלם; נבדק שוב אחרי התיקון. ראה `screenshots/2026-08-20-crowd-marker-fix.png` |
| גבולות הזירה (WALL/WALL_TOP/FRAME_TOP) — אין חיתוך ראשים, אין דמות מחוץ לתחום | ✅ | נבדק ב-15 ריצות בוט מלאות: 0 חריגות גבול |
| מניעת כפילות סטיקרים באותה ריצה | ✅ | נבדק ב-15 ריצות: 0 כפילויות |
| הריפו הציבורי `avishaicohen1112/yehali-stickers` + GitHub Pages | ✅ | נבדק ב-curl + fetch מהדפדפן: כל הקבצים 200, index.html/manifest.json/game.js תקינים |
| המשחק החי ב-GitHub Pages רץ בפועל (10/10 סטיקרים, כל הלוגיקה) | ✅ | נבדק ישירות על https://avishaicohen1112.github.io/yehali-stickers/ — לא רק לוקלית |
| manifest.json תקין (JSON תקף, שם="יהלי והסטיקרים", 4 רשומות אייקון: `any`+`maskable` נפרדים, מצביעות ל-`*-v4.png`) | ✅ | נבדק עם `json.load` + fetch חי מהשרת אחרי הפוש האחרון |
| כל 31 הקבצים שה-service worker מנסה לשמור בקאש (`FILES` ב-`sw.js`, `CACHE='yehali-stickers-v5'`) | ✅ | כל אחד נבדק בנפרד — 200 OK |
| אייקון ה-maskable (`icon-192/512-maskable-v4.png`) בתוך אזור הבטוח (66%) | ✅ | נבדק פרוגרמטית עם overlay של מעגל ה-66% על התמונה שנוצרה — התוכן כולו בפנים |
| הפעלת המשחק מקומית ב-Windows (`פתח את המשחק.bat`) | ✅ | הורץ, Edge נפתח, תהליך msedge אומת פעיל |

## שבור / חלקי

| מה | ודאות | פירוט |
|---|---|---|
| אייקון PWA על מסך הבית של המשתמש בפועל (v4: any+maskable נפרדים, פורטרט עגול בלי כיתוב מוטבע בתוך אזור הבטוח) | 🔶 | הקוד/הקבצים אומתו בשרת (200, תוכן נכון, נבדק שהתמונה בתוך מעגל 66%). **עדיין לא אומת מול המכשיר האמיתי** — הגרסה הקודמת (v3) כן נבדקה בפועל בטלפון ונמצאה חתוכה (מצח למעלה, כיתוב למטה) — זה מה שהוביל לתיקון הזה. ראה `00-הנדאוף.md` ו-`00-יומן-החלטות.md`. |
| רישום ה-service worker | ❓ | לא ניתן לאמת דרך כלי הבדיקה הפנימי (`mcp__Claude_Browser`) — הוא רץ בסביבת Electron מבודדת שחוסמת `navigator.serviceWorker.register()` עם שגיאה גנרית ("An unknown error occurred when fetching the script"), גם עבור sw.js טריוויאלי. ייתכן שזה תקין בדפדפן אמיתי (Chrome/Edge בטלפון/מחשב) — לא נבדק שם ישירות, רק הוסק מכך שכל קבצי ה-precache מחזירים 200. |

## פתוח — לא התחיל

- [ ] אישור סופי מהמשתמש שהאייקון v4 (any+maskable נפרדים, בלי חיתוך) מוצג נכון בטלפון אחרי מחיקת נתוני האתר מלאה + התקנה מחדש
- [ ] החלטה על הכיתוב הפוגעני ב-`st_million.jpg` (להשאיר/להחליף/להסיר)
- [ ] עדכון `README.md` — עדיין אומר "double-click index.html" בלי להזכיר את קישור GitHub Pages, את ה-PWA, או את בעיית שיוך ה-IE

## אזורים שלא נוגעים בהם

- `יהלי לוין - שלח בוואטסאפ.html` — קובץ גיבוי (כל המשחק + תמונות מוטמעות ב-base64, 2.7MB), נשמר כאופציה ב' לשליחה בוואטסאפ אם GitHub Pages ייפול. לא בגיט, לא מקושר מה-README.
- `פתח את המשחק.bat` — פתרון עוקף לבעיית שיוך `.html`→IE **במחשב הספציפי של המשתמש**. Windows-only, לא רלוונטי לגיטהאב, בכוונה לא נדחף.
- `WhatsApp Image 2026-08-20 at 01.10.08.jpeg` — התמונה המקורית הגולמית של יהלי. בכוונה לא נדחפה לריפו הציבורי (רק גרסאות האייקון המעובדות/הממותגות נדחפו) — שמירה על פרטיות מסוימת של התמונה הגולמית.
- `logo-יהלי-והסטיקרים.png` — לוגו 1024×1024 עצמאי שנשלח למשתמש, לא חלק מהאתר.
