# הנדאוף לשיחה הבאה

> עודכן: 2026-08-20 | השיחה הקודמת: בנתה מאפס משחק דפדפן ("יהלי לוין: בריחה מהסטיקרים"), פרסמה אותו ב-GitHub Pages, הוסיפה תמיכת PWA, ועברה 4 איטרציות על אייקון האפליקציה — כולל תיקון סופי (v4) של חיתוך אמיתי שנצפה בטלפון (ר' "מה חדש" למטה).

## הפעולה הבאה

**לשאול את המשתמש: "האם האייקון (v4) נראה נכון עכשיו בטלפון — בלי חיתוך למעלה או למטה?"**

**מה חדש בסיבוב הזה:** המשתמש שלח צילומי מסך מהטלפון שהראו חיתוך אמיתי באייקון v3 — גם המצח למעלה וגם הכיתוב למטה. הסיבה: תמונה+טקסט מילאו את כל שטח האייקון עד הקצוות, וללאנצ'ר של הטלפון (Samsung One UI) יש "אזור בטוח" של רק כ-66% מקוטר האייקון למיסוך אדפטיבי — כל מה שמחוץ לזה נחתך. פירוט מלא + הקרופים שנוסו ונכשלו: `00-יומן-החלטות.md` § "תיקון חיתוך האייקון". תוקן: `manifest.json` עכשיו מצביע על שני קבצי אייקון נפרדים — `icon-*-v4.png` (`purpose: any`, זהה ל-v3) ו-`icon-*-maskable-v4.png` (`purpose: maskable`, פורטרט עגול חדש בלי כיתוב מוטבע, ממורכז בתוך אזור ה-66%). **אומת בשרת** (curl + fetch על כל 4 קבצי האייקון + manifest.json, הכל 200 ותוכן נכון) וגם **אומת פרוגרמטית שהתמונה בתוך אזור הבטוח** (overlay של מעגל 66% על ה-PNG שנוצר). **לא אומת עדיין מול המכשיר האמיתי** — זו הפעולה שממתינה.

בסיבוב הקודם (v3), מה שבסוף גרם לרענון בטלפון היה מחיקת נתוני האתר המלאה מ-Chrome (לא רק הסרת קיצור) — הצעד הזה כבר בוצע פעם אחת בהצלחה, ולכן צריך רק לחזור עליו (מחיקת נתונים → הסרת קיצור ישן אם יש → כניסה מחדש → התקנה) בלי להסביר אותו מחדש, אלא אם המשתמש מדווח שוב שזה תקוע.

## מה חובה לדעת לפני שנוגעים

- **שינוי אייקון/קובץ סטטי כלשהו שה-PWA כבר מותקן אצל משתמשים = חובה שם קובץ חדש**, לא רק תוכן חדש באותו שם. ר' `00-גלוסרי.md` → `*-v4.png`. וגם להעלות את `CACHE` ב-`sw.js`.
- **אייקון ל-`purpose: maskable` חייב שכל התוכן החיוני שלו (פנים, טקסט אם יש) יישאר בתוך מעגל מרכזי של כ-66% מקוטר האייקון** — לא למלא את הריבוע עד הקצוות. ר' `00-גלוסרי.md` → "maskable icon / אזור בטוח". זו הייתה הסיבה האמיתית לחיתוך שנצפה בטלפון, לא בעיית קאש.
- **`WALL_TOP` (100) חייב להיות > `FRAME_TOP` (32) ב-`js/config.js`** — שינוי אחד בלי השני שובר את חיתוך הראשים בקצה העליון של הזירה.
- **`js/game.js`: `player.drawMarker()` נקרא אחרון**, אחרי כל שאר הדמויות — אל תזיזו את סדר הציור, אחרת יהלי נעלם בערימה.
- **`יהלי לוין - שלח בוואטסאפ.html`, `פתח את המשחק.bat`, `חומרי-רקע/` (תמונת מקור + לוגו)** — כולם בכוונה **לא** בגיט (untracked). אל תוסיפו אותם ל-`git add -A` בלי לבדוק את `00-סטטוס.md` → "אזורים שלא נוגעים בהם" קודם. שני הקבצים הראשונים נשארים בשורש; תמונת המקור והלוגו הועברו ב-2026-08-20 לתיקייה `חומרי-רקע/` (ר' `00-מפת-ניווט.md`).
- **`assets/stickers-video/`** — תיקייה חדשה, ריקה, לסטיקרי-וידאו עתידיים שהמשתמש יעלה. עדיין אין קוד שמטפל בהם (`js/config.js`/`js/assets.js` מכירים רק סטיקרי jpg סטטיים) — אל תניחו שהם כבר נתמכים.
- **הריפו ציבורי בגיטהאב** (`avishaicohen1112/yehali-stickers`) — כל push הופך מיד לחלק מהאתר החי. GitHub Pages בונה תוך ~20-60 שניות (ר' `00-יומן-החלטות.md` איך לבדוק בפולינג עם curl).
- **כלי הבדיקה הפנימי (`mcp__Claude_Browser`) לא יכול לאמת service worker** — רץ בסביבת Electron שחוסמת `serviceWorker.register()` תמיד. אל תבזבזו זמן לאבחן את זה מחדש — זו מגבלת כלי, לא באג.

## מה כבר ניסינו ונכשל

- הפצה דרך Claude Artifact — נדחה ע"י המשתמש (דורש חשבון קלוד).
- אימות service worker דרך הדפדפן הפנימי (`javascript_exec`) — נכשל תמיד עם "unknown error fetching script", גם על sw.js טריוויאלי לבדיקה. לא ניתן לתקן, זו מגבלת סביבת הבדיקה.
- עדכון אייקון באותו שם קובץ (v1→v2) — לא הגיע למכשיר גם אחרי הסרה+התקנה+ריסטרט. הפתרון: שינוי שם קובץ (v3).
- `direction='rtl'` ב-Pillow text — זורק KeyError (אין libraqm מותקן). הפתרון: `text[::-1]` ידני.
- `seguibl.ttf` (Segoe UI Black) לכיתוב עברי על אייקון — מרנדר תיבות ריקות, אין תמיכת עברית בפונט הזה. הוחלף ב-`segoeuib.ttf`.

## מצב בקצרה

- ✅ המשחק עצמו — כל המכניקות, נבדק בבוט אוטומטי ובעין, אפס שגיאות ב-15+ ריצות
- ✅ הריפו החי ב-GitHub Pages, כל הקבצים מוגשים נכון
- 🔶 האייקון v4 (any+maskable נפרדים) — נכון בשרת ומאומת פרוגרמטית שבתוך אזור הבטוח, לא מאומת עדיין במכשיר אמיתי (**זה הצעד הבא**)
- ❓ service worker — לא ניתן לאימות בכלי הבדיקה, רק הוסק מ-200 על כל קובץ ב-precache

פירוט מלא: `00-סטטוס.md`

## שאלות/החלטות פתוחות נוספות

הרשימה המלאה (מלבד הפעולה הבאה למעלה) מתוחזקת במקום אחד בלבד: `00-סטטוס.md` → סעיף "פתוח — לא התחיל". לא משוכפלת כאן כדי שלא ייווצרו שתי גרסאות שסותרות זו את זו.
