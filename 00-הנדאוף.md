# הנדאוף לשיחה הבאה

> עודכן: 2026-08-20 | השיחה הקודמת: הרצת ביצוע ארוכה — נוספו 5 מפות עם מצלמה עוקבת, מערכת
> גביעים ומטבעות, חנות קוסמטית, 3 סוגי אויב חדשים, 3 שדרוגים חדשים, מערכת נדירות וסטים
> לסטיקרים, ומנגנון איסוף חדש. בנוסף אותר ותוקן באג הכפתורים החתוכים אחרי מוות.

## הפעולה הבאה

**לתת למשתמש לשחק בפועל ולשמוע מה מרגיש לא נכון.** כל המערכות נבדקו בבוט אוטומטי ואפס
שגיאות — אבל **אף אדם עוד לא שיחק בזה**. הדברים שהכי סביר שיצטרכו כוונון (ומסומנים 🔶
ב-`00-סטטוס.md`): עלויות החנות, ספי הגביעים למפות, וקצב הופעת הסטיקרים לאיסוף.

**שתי הודעות שחייבים למסור למשתמש אם עוד לא נמסרו:**
1. בטעות דחפתי לרגע לריפו הציבורי את ארבעת הקבצים הפרטיים (כולל תמונת המקור). תוקן תוך
   דקה, ההיסטוריה נקייה, ונוצר `.gitignore` — אבל הם היו חשופים ~60 שניות.
2. הבאג שהוא דיווח עליו ("אין איך לחזור לבית אחרי מוות") **תוקן**, וזה לא היה כפתור חסר
   אלא גלישה במסך נמוך — ר' למטה.

## מה חובה לדעת לפני שנוגעים

- **`CFG.W/H` = גודל התצוגה. `CFG.worldW/worldH` = גודל המפה.** כל לוגיקת עולם (גבולות,
  הופעות, קליעים) עובדת מול `worldW/H`. `Game.setMap()` דורס אותם. **אם משהו מופיע במקום
  הלא נכון — כמעט תמיד בלבלו בין השניים.**
- **מה בקואורדינטות עולם ומה בקואורדינטות מסך:** כל מה שבתוך `drawWorld` הוא *עולם*
  (כולל כל אפקטי FX). סטיקרים על המסך, HUD, באנרים ו-`drawSlap` הם *מסך*. המרה: `−cam.x/y`.
- **שינוי אייקון/CSS/JS ב-PWA מותקן = חובה להעלות את `CACHE` ב-`sw.js`.** זה לא תיאורטי:
  בזמן הפיתוח ה-SW הגיש CSS ישן ובזבז זמן אבחון. כרגע `v7`.
- **`WALL_TOP` (100) חייב להישאר > `FRAME_TOP` (32)** ב-`js/config.js`.
- **`player.drawMarker()` נקרא אחרון** ב-`js/game.js` — אחרת יהלי נעלם בערימה.
- **אל תשתמשו ב-`git add -A` בלי לבדוק `git status` קודם.** יש `.gitignore` עכשיו, אבל
  הרשימה של מה שאסור לדחוף חיה ב-`00-סטטוס.md` → "אזורים שלא נוגעים בהם".
- **הסלון (מפה 0) הוא הבנצ'מרק של האיזון** — הוא נשאר 1280×720 עם יחס צפיפות 1.0 בכוונה,
  כדי לא לשבור את הכוונון האמפירי שנעשה עם בוט. אל תשנו אותו בלי סיבה טובה.

## איך בודקים בלי מסך

הדפדפן הפנימי לא מצייר כשהחלונית מוסתרת, ולכן `requestAnimationFrame` לא רץ ו**המשחק
נראה תקוע**. הפתרון שעבד: להריץ את הלולאה ידנית מ-JS —
`Game.update(dt)` + `Game.render()` בלולאה, עם בוט שמזין `Input.axis`.
ככה נבדקו כל המפות והמערכות, וזה גם מהיר בהרבה מזמן אמת. שרת מקומי: `.claude/launch.json`.

## מה כבר ניסינו ונכשל

- הפצה דרך Claude Artifact — נדחה ע"י המשתמש (דורש חשבון קלוד).
- אימות service worker דרך הדפדפן הפנימי — נכשל תמיד (מגבלת Electron, לא באג).
- עדכון אייקון באותו שם קובץ — לא מגיע למכשיר. חייב שם חדש.
- `direction='rtl'` ב-Pillow — `KeyError` (אין libraqm). הפתרון: `text[::-1]`.
- `seguibl.ttf` לעברית — מרנדר תיבות ריקות. להשתמש ב-`segoeuib.ttf`.
- **חיפוש הבאג של "אין איך לחזור לבית" בקוד ה-JS** — בזבוז זמן: הכפתור היה קיים ומחובר
  כל הזמן. הבעיה הייתה **CSS** — `place-items:center` בלי `overflow` הוציא את הכפתורים
  מהמסך בטלפון לרוחב. **הלקח: באג "כפתור לא נגיש" צריך להיבדק קודם כל בגדלי מסך שונים.**

## מצב בקצרה

- ✅ המשחק, 5 המפות, המצלמה, 7 האויבים, השדרוגים, הכלכלה, החנות, הנדירות — כולם נבדקו ועובדים
- ✅ ביצועים: 5.5ms לפריים בעומס קיצוני (פי 3 מרווח)
- 🔶 המספרים (עלויות/ספים/קצב) — נבדקו בבוט, לא מול אדם
- 🔶 האייקון בטלפון — עדיין לא אושר ע"י המשתמש
- ❌ סטיקרי וידאו — התיקייה קיימת, אין קוד שמנגן

פירוט מלא: `00-סטטוס.md` · מה הלאה: `00-לוז.md`

## שאלות פתוחות למשתמש

הרשימה המלאה מתוחזקת ב-`00-סטטוס.md` → "פתוח — לא התחיל", ולא משוכפלת כאן.
הבולטות: העלאת הסטיקרים החדשים עם סט+דרגה, והחלטה על `st_million.jpg`.
