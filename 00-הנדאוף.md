# הנדאוף לשיחה הבאה

> עודכן: 2026-08-20 | השיחה הקודמת: הרצת ביצוע ארוכה (המשתמש לא היה זמין) — נוספו 5 מפות עם
> מצלמה עוקבת, גביעים+מטבעות+חנות, 3 אויבים חדשים, 3 שדרוגים חדשים, מערכת נדירות+סטים
> לסטיקרים (עם מנגנון איסוף חדש), ותוקן באג הכפתורים החתוכים אחרי מוות. בסוף השיחה נוספה גם
> **התראת עדכון PWA** ("יש גרסה חדשה" + כפתור רענון) כדי שלא יצטרך להסיר+להתקין מחדש בכל גרסה.

## הפעולה הבאה

**לתת למשתמש לשחק בפועל ולשמוע מה מרגיש לא נכון.** כל המערכות נבדקו בבוט אוטומטי ואפס
שגיאות — אבל **אף אדם עוד לא שיחק בזה בפועל**. החשודים הראשונים לכוונון (מסומנים 🔶
ב-`00-סטטוס.md`): עלויות החנות, ספי הגביעים למפות, קצב הופעת הסטיקרים לאיסוף.

**בנוסף, בפעם הבאה שעולה גרסה חדשה** — כדאי לוודא מול המשתמש שהתראת "יש עדכון" *באמת*
מופיעה לו על האייפון בפועל (נבדקה רק בסימולציה בכלי הבדיקה הפנימי, לא על מכשיר אמיתי).

## מה חובה לדעת לפני שנוגעים

- **`CFG.W/H` = גודל התצוגה (קבוע). `CFG.worldW/worldH` = גודל המפה הנוכחית**, נדרס ע"י
  `Game.setMap()`. כל לוגיקת עולם (גבולות, הופעות, קליעים) עובדת מול `worldW/H`. **אם משהו
  מופיע במקום הלא נכון — כמעט תמיד בלבלו בין השניים.**
- **קואורדינטות עולם מול מסך:** כל מה שבתוך `drawWorld` הוא *עולם* (כולל FX). סטיקרים על
  המסך, HUD, באנרים ו-`drawSlap` הם *מסך*. המרה: `−cam.x/y`.
- **כל שינוי ב-`index.html`/`css/style.css`/`js/*.js` בפרויקט הזה = חובה להעלות את `CACHE`
  ב-`sw.js`.** לא תיאורטי — זה כבר קרה בפועל פעמיים בשיחה הזו (הגיש CSS ישן, בזבז זמן אבחון).
  כרגע `v8`. **עם עדכון ה-`CACHE`, המשתמש כבר לא צריך להסיר+להתקין מחדש** — יופיע לו באנר
  "יש עדכון" ולחיצת רענון תספיק (ר' `00-גלוסרי.md` → `hadController`).
- **`WALL_TOP` (100) חייב להישאר > `FRAME_TOP` (32)** ב-`js/config.js`.
- **`player.drawMarker()` נקרא אחרון** ב-`js/game.js` — אחרת יהלי נעלם בערימה.
- **אל תשתמשו ב-`git add -A` בלי `git status` קודם.** יש `.gitignore` שמגן על 4 הקבצים
  הפרטיים (ר' `00-סטטוס.md` → "אזורים שלא נוגעים בהם") — אבל עדיין, לבדוק לפני שדוחפים.
- **הסלון (מפה 0) הוא הבנצ'מרק של האיזון** — 1280×720, יחס צפיפות 1.0 בכוונה. אל תשנו אותו
  בלי סיבה טובה — זה ישבור את הכוונון האמפירי שכבר נבדק עם בוט.

## איך בודקים בלי מסך

הדפדפן הפנימי לא מצייר כשהחלונית מוסתרת, אז `requestAnimationFrame` לא רץ והמשחק נראה תקוע.
פתרון שעבד: להריץ את הלולאה ידנית מ-JS — `Game.update(dt)` + `Game.render()` בלולאה, עם בוט
שמזין `Input.axis`. כך נבדקו כל המפות/המערכות, וזה גם מהיר בהרבה מזמן אמת. שרת מקומי:
`.claude/launch.json` (`python -m http.server`). עדכון SW נבדק בדומה — `reg.update()` +
האזנה ל-`controllerchange` ידנית, בלי לחכות לבדיקה האמיתית של הדפדפן.

## מה כבר ניסינו ונכשל

- הפצה דרך Claude Artifact — נדחה ע"י המשתמש (דורש חשבון קלוד).
- אימות service worker/עדכונים דרך הדפדפן הפנימי — לא רץ לבד (מגבלת Electron, לא באג);
  צריך להריץ ידנית (ר' למעלה).
- עדכון אייקון/נכס באותו שם קובץ — לא מגיע למכשיר. חייב שם/CACHE חדש.
- `direction='rtl'` ב-Pillow — `KeyError` (אין libraqm). הפתרון: `text[::-1]`.
- `seguibl.ttf` לעברית — מרנדר תיבות ריקות. להשתמש ב-`segoeuib.ttf`.
- **חיפוש הבאג "אין איך לחזור לבית" בלוגיקת JS** — בזבוז זמן: הכפתור היה קיים ומחובר כל
  הזמן. הבעיה הייתה CSS (`place-items:center` בלי `overflow` בטלפון לרוחב). **הלקח: באג
  "כפתור לא נגיש" — לבדוק קודם כל בגדלי מסך שונים, לא בקוד ה-JS.**
- רענון אוטומטי-כפוי עם `location.reload()` מיד ב-`controllerchange` — נשקל ונדחה, כי זה
  יכול לקטוע ריצה באמצע משחק. נבחר: התראה + כפתור, המשתמש בוחר מתי.

## מצב בקצרה

- ✅ המשחק, 5 המפות, המצלמה, 7 האויבים, השדרוגים, הכלכלה, החנות, הנדירות, התראת העדכון —
  כולם נבדקו ועובדים (בבוט/בסימולציה)
- ✅ ביצועים: 5.5ms לפריים בעומס קיצוני (פי 3 מרווח מ-60fps)
- 🔶 המספרים (עלויות/ספים/קצב) — נבדקו בבוט, לא מול אדם — **זה הפער הכי גדול כרגע**
- 🔶 האייקון בטלפון, והתראת העדכון על מכשיר אמיתי — שניהם עדיין לא אושרו ע"י המשתמש
- ❌ סטיקרי וידאו — התיקייה קיימת, אין קוד שמנגן

פירוט מלא: `00-סטטוס.md` · מה הלאה: `00-לוז.md`

## שאלות פתוחות למשתמש

הרשימה המלאה ב-`00-סטטוס.md` → "פתוח — לא התחיל", לא משוכפלת כאן. הבולטות: העלאת
הסטיקרים החדשים עם סט+דרגה, והחלטה על `st_million.jpg`.
