# גלוסרי

> מונחים, שמות וקבועים שחוזרים בפרויקט. מצטבר — מוסיפים, לא מוחקים.

| מונח | מה זה | איפה | למה חשוב |
|---|---|---|---|
| WALL / WALL_TOP / FRAME_TOP | שלושה קבועים נפרדים לגבולות הזירה: WALL=מסגרת שמאל/ימין/למטה (26), WALL_TOP=עד לאן דמות מותרת לזוז למעלה (100), FRAME_TOP=איפה מצוירת קו המסגרת החזותי (32) | `js/config.js` | WALL_TOP חייב להיות **גדול** מ-FRAME_TOP. אם משנים רק אחד מהם — ראשי דמויות (במיוחד 'טנק', scale גדול) יבלטו מחוץ למסגרת המצוירת או ייחתכו. תוקן פעם אחת אחרי באג שנצפה בפועל. |
| drawMarker | פונקציה ב-`js/entities.js` (מחלקת Player) שמציירת טבעת+חץ זהב מעל יהלי | `js/entities.js` | נקראת **אחרונה**, אחרי כל שאר הדמויות, ב-`js/game.js`. אם מזיזים את הקריאה לפני ציור החברה — יהלי שוב ייעלם בערימה. |
| STICKER_DATA_URIS | אובייקט גלובלי (window.STICKER_DATA_URIS) שמכיל את תמונות הסטיקרים כ-base64, בשימוש רק בגרסת "קובץ יחיד לוואטסאפ" | `build_artifact.py` (בסקראצ'פד, לא בפרויקט) | קיים רק בקובץ ההרכבה החד-פעמי; ה-assets.js הרגיל בפרויקט טוען מהתיקייה `assets/stickers/` ישירות. |
| bot / pro (בדיקות) | שני פונקציות JS זמניות (`bot`, `pro`) שהוזרקו לדפדפן דרך javascript_exec לצורך סימולציית שחקן אוטומטית לאיזון קושי | לא בקוד הפרויקט — נכתבו אד-הוק בכלי בדיקה | אם רוצים לחזור ולכוונן קושי, אפשר לשחזר את הלוגיקה מ-`00-יומן-החלטות.md` (בריחה מאיום קרוב + איסוף שדרוגים + דחייה מקירות) |
| `*-v4.png` (סיומת גרסה בשמות אייקונים) | סיומת cache-busting מכוונת בשמות קבצי האייקון (`icon-192-v4.png` וכו', לשעבר `v3`) | `manifest.json`, `index.html`, `sw.js` | **אל תחזירו לשם בלי הסיומת.** שם קובץ קבוע עם תוכן משתנה נתקע מאחורי HTTP cache (`Cache-Control: max-age=600` ב-GitHub Pages) וגם מאחורי Cache Storage של ה-service worker. כל שינוי עתידי באייקון חייב שם קובץ **חדש**, אחרת מכשירים שכבר התקינו את ה-PWA לא יראו את העדכון גם אחרי הסרה+התקנה מחדש. |
| CACHE (בתוך sw.js) | קבוע string לשם ה-Cache Storage, כרגע `'yehali-stickers-v5'` | `sw.js` | חייב לעלות בכל שינוי לרשימת `FILES` או לתוכן קובץ סטטי כלשהו — זה מה שגורם ל-`activate` event למחוק קאש ישן ולבנות מחדש. |
| maskable icon / אזור בטוח (safe zone) | קטגוריית `purpose` ב-`manifest.json` להבדיל בין אייקון "כמו שהוא" (`any`) לאייקון שעשוי לעבור מיסוך אדפטיבי (`maskable`) על מסך הבית. ב-`maskable`, רק תוכן שנמצא בתוך מעגל מרכזי שהוא כ-66% מקוטר האייקון מובטח להישאר גלוי — כל מה שמחוץ לו עלול להיחתך ע"י הלאנצ'ר | `manifest.json` → `icons[].purpose`, `icon-*-maskable-v4.png` | **גילינו בדרך הקשה:** אייקון אחד ששימש גם `any` וגם `maskable` יחד, עם תמונה+טקסט שממלאים את כל הריבוע עד הקצה — נחתך בפועל בטלפון Samsung (מצח למעלה, כיתוב למטה). הפתרון: קובץ **נפרד** ל-`maskable` שכל התוכן שלו (התמונה, בלי שום טקסט מוטבע) ממורכז בתוך מעגל ה-66%. ר' `00-יומן-החלטות.md` § "תיקון חיתוך האייקון". |
| Samsung One UI (לאנצ'ר) | שכבת ה-UI של סמסונג מעל אנדרואיד; מסכת ה-squircle שלה למסך הבית חותכת יותר אגרסיבית מהמינימום הרשמי של Android (66%) | — | הטלפון שעליו כל בדיקות האייקון בוצעו בפועל. אם בעתיד רוצים לבדוק על אנדרואיד "טהור" (Pixel וכו') — ייתכן שהחיתוך פחות מורגש שם, אבל אין לסמוך על זה; לתכנן תמיד לפי ה-66% הרשמי. |
| libraqm (חוסר) | ספריית עיצוב טקסט ל-Pillow, **לא מותקנת** בסביבה של המשתמש | סקריפטים ב-scratchpad שיצרו אייקונים (`make_photo_icon2.py`) | בלעדיה, `ImageDraw.text(..., direction='rtl')` זורק `KeyError`. הפתרון שנעשה: להפוך את המחרוזת ידנית (`text[::-1]`) לפני ציור — עובד טוב לעברית פשוטה בלי ניקוד/מספרים מעורבים, אבל **לא** יעבוד נכון אם יתערבב טקסט אנגלי/מספרים בתוך המחרוזת העברית. |
| segoeuib.ttf | הפונט שנבחר לכיתוב על האייקונים (Segoe UI Bold) | `C:\Windows\Fonts\segoeuib.ttf`, בשימוש ב-scripts יצירת אייקונים | **לא** `seguibl.ttf` (Segoe UI Black) — לפונט הזה אין glyphs לעברית בכלל (מרנדר תיבות ריקות/tofu). segoeuib נבחר גם כי הוא אותו family שמוגדר ב-`--font` ב-`css/style.css`, לעקביות מותגית. |
| gradient_text() | פונקציה בסקריפט יצירת האייקון שמייצרת גרדיאנט אנכי לבן→זהב(#ffd977)→כתום(#ff9d1f) ומשתמשת בטקסט כמסכת alpha | `make_photo_icon2.py` (בסקראצ'פד) | זהו שחזור ידני ב-Pillow של אפקט ה-CSS `.logo-main` (`background:linear-gradient(...); -webkit-background-clip:text`) שכבר קיים ב-`css/style.css`. אם רוצים לשנות את הצבעים — לשנות בשני המקומות כדי לשמור עקביות. |
| WhatsApp Image 2026-08-20 at 01.10.08.jpeg | התמונה המקורית (לא ערוכה) של יהלי אוכל גלידה, ששימשה בסיס לאייקון | שורש הפרויקט | קובץ מקור. **לא נדחף לגיטהאב** בכוונה — רק גרסאות האייקון המעובדות ממנה נדחפו. |
