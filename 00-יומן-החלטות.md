# יומן החלטות

> מצטבר. החדש למעלה. תאריכים מוחלטים בלבד.

## 2026-08-20 (ביצוע) — מפות, כלכלה, אויבים ושדרוגים חדשים

הרצת ביצוע ארוכה שבה המשתמש לא היה זמין. ארבעה אישורים ניתנו מראש: push חופשי,
**קוד בלבד בלי ג'מיני** (יעבור לסשן נפרד), מפות עם משחק שונה ולא רק מראה, וגביעים שעולים ויורדים.

**החלטות:**

- **מצלמה עוקבת במקום להקטין את הזירה** — **למה:** "מפה גדולה יותר" בלי מצלמה פירושו לדחוס
  עולם גדול לאותו קנבס, כלומר הכל נהיה קטן יותר ופחות קריא. מצלמה שומרת על גודל הדמויות
  ומגדילה את העולם באמת. **נדחה:** להגדיל את `CFG.W/H` — היה מקטין את כל הדמויות.
  **מימוש:** `CFG.W/H` נשארו גודל *התצוגה*, ונוספו `CFG.worldW/worldH` לגודל *המפה*.
  ההפרדה שכבר הייתה בקוד בין `drawWorld` (בתוך טרנספורם) לבין HUD/סטיקרים (מחוץ לו) התאימה
  לזה בדיוק — לכן שכבות המסך לא נגעו במצלמה בכלל.

- **חיצים בקצה המסך למה שמחוץ לתצוגה** — **למה:** במפה גדולה מהמסך, לחטוף ממישהו שלא ראית
  זה פשוט לא הוגן. החיצים מסמנים חברה קרובים, שדרוגים וסטיקרים לאיסוף.

- **צפיפות החברה גדלה עם שטח המפה** (`sqrt` של יחס השטחים, תקרה ×1.85) — **למה:** אותה כמות
  חברה במפה פי-3 גדולה הופכת את המפה הגדולה ל*קלה* יותר וריקה, כלומר ההתקדמות מרגישה הפוכה.
  הסלון נשאר בדיוק כפי שהיה (יחס 1.0) כדי לא לשבור את האיזון שנבדק אמפירית.

- **הופעת חברה בטבעת סביב השחקן ולא על היקף המפה** — **למה:** במפה גדולה, הופעה בפינה
  הרחוקה מבזבזת חצי גל בהליכה והזירה מרגישה ריקה.

- **פתיחת סטיקרים באוסף עברה מ"חטיפה" ל"איסוף"** — **למה:** בקשה מפורשת של המשתמש: "עדיף
  שתשיג אם תנצח". העונש החזותי (סטיקר נדבק על המסך) נשאר בדיוק כפי שהיה. השורות שפתחו את
  הגלריה ב-`takeHit` הועברו ל-`collectSticker`.

- **נדירות = משקל הדרגה × מכפיל הסט** — **למה:** כך "רגיל" בסט נדיר באמת נדיר יותר מ"רגיל"
  בסט רגיל, בדיוק כפי שהמשתמש ביקש, בלי טבלה נפרדת לכל צירוף. נמדד בפועל על 8,000 הגרלות:
  90.8% / 6% / 2.1% / 0.8% / 0.2% — אולטרה סיישל יוצא אחד ל-500.

- **משקלי הגרלה לשדרוגים (`weight`)** — **למה:** עד עכשיו לשדרוגים לא היה שדה כזה בכלל
  (לאויבים כן היה), ולכן כולם הופיעו בהסתברות שווה ולא היה *שום* מקום לעשות נרף לפאנישר.
  פאנישר ירד ל-4 מתוך 46 (8.7%, במקום ~33% קודם). פיצה+גולאש ביחד = 37% מההגרלות, כמענה ישיר
  לבעיה שהמשתמש העלה — שאין מספיק דרכים להיפטר מאויבים כשהספאון לא נעצר.

- **גולאש הוא שדרוג מיידי (`dur: 0`) ולא מתמשך** — **למה:** אגרוף בטן שמפיל את כל מי שקרוב
  הוא רגע אחד, לא מצב. `dur: 0` הוא הדפוס שכבר קיים במשחק לוואגיו ולסיישל.

- **יהלי פיצה זורק לבד, בלי כפתור חדש** — **למה:** להוסיף קלט חדש היה שובר את הפשטות של
  "תזוזה + דאש" ואת פקדי המגע בנייד. הוא מכוון לקרוב ביותר בטווח 460.

- **`align-content: safe center` לתיקון הכפתורים החתוכים** — ר' "התיישן" למטה.

**שינויים בפועל:**
- `js/config.js` — `maps` (5), `worldW/H`, `cam`, 3 אויבים חדשים, `power` עם משקלים + 3 שדרוגים
  חדשים, `rarity`, `sets`, `collect`, `shopItems`, `trophy`, `coin`, `yehaliTalk`, `duoLines`, מפתחות אחסון.
- `js/entities.js` — גבולות עולם, התנהגויות `blocker`/`sticky`/`hopper`, מחלקות `Puddle`,
  `Pizza`, `Collectible`, סקין/שובל מהחנות, האטה משלוליות.
- `js/game.js` — מצלמה, טעינת מפות, גביעים, מטבעות, חנות, בחירת מפה, איסוף סטיקרים,
  "בפוקס", `yehaliReply`, פיצה/גולאש, חיצים למחוץ למסך, המרות עולם↔מסך.
- `js/sprites.js` — `Art.floor` עם פלטה + ציור רק באזור הנראה, `Art.darken`, אייקוני פיצה ואגרוף.
- `index.html` / `css/style.css` — מסכי מפות וחנות, ארנק, תיקון גלילה ומדיה-קוורי למסך נמוך.
- `sw.js` — `CACHE` ל-v7 (חובה, אחרת מכשירים מותקנים מקבלים את הגרסה הישנה).
- `.gitignore` — נוצר, ר' "נשאר פתוח".

**מה נבדק בפועל (✅):** אפס שגיאות ב-5 המפות לאורך ~5 דקות סימולציה; כל 7 סוגי האויב מופיעים;
המצלמה תמיד בתוך הגבולות; כל הישויות בתוך המפה; חנות (קנייה/ציוד/הסרה/אין מספיק כסף);
נעילת מפות לפי גביעים; גביעים עולים ויורדים ולא מתחת לאפס; שמירה ל-localStorage; סט נעול-למפה
מופיע רק במפה שלו (29 פעם ברומניה, 0 בכל השאר); כל-הסטיקרים-נאספו לא קורס; ביצועים בעומס קיצוני
(20 חברה, 26 שלוליות) — 5.5ms לפריים, פי 3 מרווח מ-60fps.

**התיישן:**
- **`00-סטטוס.md` → "כפתור לתפריט (btnMenu)"** — הסתיים. הבאג **אותר ותוקן**: זו לא הייתה
  היעדרות של הכפתור (הוא היה קיים ומחובר), אלא **גלישה במסך נמוך**. ב-`.screen` היה
  `place-items:center` בלי `overflow`, ולכן בטלפון לרוחב (גובה ~330-400px) התוכן היה גבוה
  מהחלון והשורה התחתונה — בדיוק הכפתורים "לתפריט"/"עוד פעם" — יצאה מהמסך **בלי שום דרך
  לגלול אליה**. נמדד: הכפתור ישב ב-y=515 על חלון בגובה 400. תוקן ב-`align-content:safe center`
  + `overflow-y:auto` + מדיה-קוורי שמכווץ את מסך הסיום מתחת ל-460px גובה. אומת עד גובה 330px.
- **`README.md`** — עודכן במלואו (מפות, אויבים חדשים, שדרוגים, כלכלה, נדירות וסטים).

**נשאר פתוח:**
- **טעות שלי שתוקנה:** השתמשתי ב-`git add -A` ובטעות דחפתי לריפו הציבורי את ארבעת הקבצים
  שמסומנים "לא לדחוף" — כולל תמונת המקור הגולמית. תוקן תוך דקה: הקומיט נבנה מחדש בלעדיהם
  ונדחף ב-`--force-with-lease`; אומת שאף קומיט בהיסטוריה כבר לא מכיל אותם. נוצר `.gitignore`
  כדי שזה לא יוכל לחזור. **המשתמש צריך לדעת שהם היו חשופים בפומבי לכ-60 שניות.**
- כוונון מספרים סופי (עלויות חנות, ספי גביעים, קצב הופעת סטיקרים) — נקבע בהיגיון, לא נבחן
  מול שחקן אנושי אמיתי.

## 2026-08-20 (המשך) — מחקר: שדרוג ארט סטייל + ייצור תמונות עם ג'מיני

**החלטות:** אין עדיין החלטה סופית — זה סבב מחקר, לא ביצוע. המשתמש ביקש לעצור לפני יישום כדי לעשות
דברים אחרים קודם, וביקש לשמור את הידע. פירוט מלא (כולל מקורות): `מדריך-יצירת-תמונות.md`.

**ממצאי המחקר בקצרה:**
- Gemini/Nano Banana לא מייצרים PNG עם שקיפות אמיתית — צריך שיטת עקיפה (chroma key או diff matting).
- **לפרויקט הזה ספציפית, diff matting עדיף על chroma key** — כי כל צבע רווי אפשרי כבר בשימוש כצבע-דמות
  בפלטה של `css/style.css`, אז מסך-כחול/ירוק עלול "לאכול" חלקים מהדמות עצמה.
- הגישה המומלצת לשדרוג הדמויות: תמונות סטטיות (3-5 תנוחות לדמות), לא אנימציה מיוצרת אוטומטית — נטען
  באותו מנגנון `drawImage` שכבר קיים לסטיקרים, בלי לשבור את "וניל בלי dependencies".

**שינויים בפועל:** רק תיעוד — `מדריך-יצירת-תמונות.md` (2 סעיפים חדשים), `00-לוז.md`, `00-גלוסרי.md`.
שום קוד לא שונה, שום קריאת API לא בוצעה.

**התיישן:** כלום.

**נשאר פתוח:**
- אישור המשתמש על diff-matting כשיטת הרקע-השקוף, לפני ייצור תמונה ראשונה בפועל.
- **אסור לקרוא ל-API של ג'מיני (`Gemini Project` / `gen-lang-client-0874527148`) בלי אישור מפורש
  בכל פעם** — המשתמש ביקש זאת במפורש, זו עלות אמיתית.
- החלטה סופית על סגנון האמנות של הדמויות (עדיין רק כיוון מומלץ, לא סגור).

## 2026-08-20 (המשך) — ארגון קבצים בשורש + תיקיית סטיקרי-וידאו

**החלטות:**
- **תמונת המקור והלוגו עברו משורש הפרויקט לתיקייה `חומרי-רקע/` (עם תת-תיקיות `תמונות-מקור/` ו-`לוגו/`), ושונה שמם** — **למה:** המשתמש דיווח ששורש הפרויקט "מבולגן" מתמונות זרוקות. שני הקבצים לא מקושרים מקוד או מ-manifest (רק קבצי `icon-*`/`favicon`/`apple-touch-icon` מקושרים, ואלה **לא** הוזזו) — כך שהעברה ושינוי שם בטוחים לגמרי, לא שוברים כלום. **נדחה:** מחיקה — המשתמש ביקש ארגון, לא הסרה; שני הקבצים עדיין בכוונה לא בגיט (ר' `00-סטטוס.md`).
- **נוצרה `assets/stickers-video/` ריקה, ליד `assets/stickers/`** — **למה:** בקשת המשתמש, מיועדת לסטיקרי-וידאו שיועלו בהמשך כתכונה עתידית של המשחק. **נדחה:** כלום — זו תיקייה מוכנה מראש, אין עדיין קוד שמטפל בה.

**שינויים בפועל:**
- `WhatsApp Image 2026-08-20 at 01.10.08.jpeg` → `חומרי-רקע/תמונות-מקור/יהלי-תמונת-מקור.jpeg`
- `logo-יהלי-והסטיקרים.png` → `חומרי-רקע/לוגו/לוגו-ראשי.png`
- תיקייה חדשה וריקה: `assets/stickers-video/`
- עודכנו כל האזכורים של הקבצים הישנים ב-`00-סטטוס.md`, `00-מפת-ניווט.md`, `00-גלוסרי.md`, `00-הנדאוף.md`

**נשאר פתוח:**
- כשיועלו קבצי הווידאו בפועל ל-`assets/stickers-video/` — יידרש שינוי קוד ב-`js/config.js`/`js/assets.js` כדי שהמשחק בכלל יכיר סוג-מדיה כזה (כרגע נתמכים רק jpg סטטיים).

## 2026-08-20 (המשך) — תיקון חיתוך האייקון: maskable נפרד מ-any

**הבעיה שהתגלתה:** המשתמש דיווח (עם צילומי מסך מהטלפון) שהאייקון v3 עדיין נחתך בפועל — גם המצח למעלה וגם הכיתוב למטה — למרות ששינוי שם הקובץ (v3) פתר את בעיית הקאש התקוע. אבחון: ב-`manifest.json` הישן, אותה תמונה אחת שימשה גם ל-`purpose: "any"` וגם ל-`"maskable"`, עם התמונה+הכיתוב ממלאים את כל השטח עד הקצוות. לאנצ'רים עם מיסוך אדפטיבי (בעיקר Samsung One UI, ר' `00-גלוסרי.md`) חותכים כל מה שמחוץ ל"אזור הבטוח" — מעגל שהוא רק כ-66% מקוטר האייקון. תמונה+טקסט שממלאים את כל הריבוע יוצאים מהמעגל הזה ונחתכים, בלי קשר לשם הקובץ.

**החלטה: שני קבצי אייקון נפרדים ב-manifest, לא אחד משותף** — **למה:** זו הדרך התקנית שה-spec של Android מתאר בדיוק בשביל המקרה הזה — `purpose: "any"` להקשרים לא-ממוסכים (favicon, apple-touch-icon, דסקטופ) ו-`purpose: "maskable"` נפרד להקשר שכן ממסך (מסך בית אנדרואיד). **נדחה:** לנסות לצמצם עוד יותר תמונה+טקסט לתוך מעגל 66% יחיד — נבדק (ר' "מה כבר ניסינו" למטה), יצא זעיר ולא קריא.

**החלטה: הוסר הכיתוב המוטבע מתמונת ה-maskable, נשאר רק פורטרט עגול** — **למה:** מערכת ההפעלה כבר מציגה את שם האפליקציה כתווית טקסט נפרדת מתחת לאייקון (מ-`manifest.json` → `name`/`short_name`) — זה נצפה בפועל בצילומי המסך של המשתמש (יש טקסט "יהלי והסטיקרים" גם בתוך התמונה וגם כתווית נפרדת מתחתיה, כפילות). הכיתוב המוטבע היה גם המקור לחיתוך התחתון. הסרתו פותרת את הבעיה לגמרי ומאפשרת תמונת פורטרט גדולה וברורה יותר בתוך אזור הבטוח. **נדחה:** להשאיר טקסט קטן מאוד בתוך המעגל — נבדק, יצא בגודל ~30px על קנבס 512, לא קריא.

**שינויים בפועל:**
- `manifest.json`: `icons` פוצל לארבע רשומות — `icon-192-v4.png`/`icon-512-v4.png` (`purpose: "any"`, זהות בתוכן ל-v3, רק שם קובץ חדש) + `icon-192-maskable-v4.png`/`icon-512-maskable-v4.png` (`purpose: "maskable"`, חדשות — פורטרט עגול ממורכז בתוך מעגל 66%, טבעת זהב דקה, בלי טקסט, על רקע `#0b0912`).
- `apple-touch-icon-v4.png`, `favicon-v4.png` — הועברו לשם v4 בלי שינוי תוכן (לעקביות גרסאות בלבד).
- `sw.js`: `FILES` עודכן לכל שמות ה-v4 (כולל שני קבצי ה-maskable החדשים), `CACHE` קפץ מ-`v4` ל-`v5`.
- כל v3 (הישנים) נמחקו מהריפו.

**מה כבר ניסינו ונכשל (בתוך הצעד הזה):**
- ניסיון ראשון: תמונה+טקסט גדולים בתוך אותו מעגל 66% (badge_d=260, font=54 על קנבס 512) — הטקסט יצא כולו מחוץ למעגל הבטוח לגמרי (נבדק עם overlay ירוק של המעגל האמיתי).
- ניסיון שני: הקטנת טקסט לפונט 30 כדי שייכנס — טכנית נכנס, אבל בגודל export של 192px זה יוצא ~11px, לא קריא. הוחלט להסיר לגמרי במקום.
- קרופ ראשון של הפורטרט (איזור x:60-1020,y:150-1110 מהתמונה המקורית) — יצא לא ממורכז, עין ימין כמעט נחתכת מהפריים. תוקן לקרופ ממורכז סביב שתי העיניים (x:300-1000, y:130-830).

**נשאר פתוח:**
- **הכי דחוף:** אימות מהמשתמש שהאייקון החדש (v4 + maskable נפרד) נראה נכון בפועל בטלפון, בלי חיתוך למעלה או למטה, אחרי מחיקת נתוני האתר מלאה ב-Chrome (לא רק הסרת קיצור).

## 2026-08-20 — בניית המשחק, הפצה, PWA ואייקון מתמונה אמיתית

**החלטות:**

- **Canvas 2D + JS וניל, בלי framework ובלי build step** — **למה:** צריך שיפתח בלחיצה אחת אצל חברים, בלי npm/build/שרת. **נדחה:** כל מנוע/framework שדורש התקנה.

- **דמות יהלי מצוירת ידנית ב-canvas (`js/sprites.js`), לא תמונות אמיתיות בתוך המשחק** — **למה:** התמונות האמיתיות (סטיקרים) הן "הפרס/העונש" של המשחק עצמו — הבדיחה שהחבר'ה מדביקים לך את הפרצוף שלך. הדמות המשחקית צריכה להיות מצוירת ואנימטיבית (הליכה, דאש, מצמוץ). הציור מבוסס על תמונות אמיתיות שהמשתמש הראה (משקפיים, שפם, זקנקן, מבנה גוף) לצורך דמיון, לא הועתק כתמונה.

- **סמן זהב (טבעת + חץ) מעל יהלי, מצויר אחרון מעל כל הדמויות** — **למה:** בבדיקות בוט עם 9 חברה על המסך, יהלי ממש נעלם בתוך הערימה חזותית. זו לא הייתה תכונה מתוכננת — זה תוקן אחרי שנצפה בפועל בבדיקה (ראה `screenshots/2026-08-20-crowd-marker-fix.png`). **נדחה:** צביעה שונה של יהלי בלבד — לא מספיק בולט מרחוק.

- **WALL_TOP (100) גבוה מ-FRAME_TOP (32) ב-`js/config.js`** — **למה:** המסגרת המצוירת של הזירה (FRAME_TOP) ורצועת התזוזה המותרת לדמויות (WALL_TOP) הן שני קבועים נפרדים בכוונה. בלי ההפרדה, ראשי הדמויות הגבוהות (כמו 'טנק') בלטו מחוץ למסגרת המצוירת כשעמדו בקצה העליון. **נדחה:** מסגרת אחידה אחת — גרמה לחיתוך ראשים.

- **איזון קושי נקבע אמפירית עם בוט-שחקן, לא בניחוש** — **למה:** הרצתי בוט שממלא Input.axis לפי חוקים (בריחה מאיום קרוב, איסוף שדרוגים) עשרות פעמים ומדד גל/זמן הישרדות ממוצע. גילינו שבוט "גרוע" מת תמיד צמוד לקיר במהירות נמוכה (100% מהמקרים) — כלומר המשחק הוגן, לא ה-AI. כוונון סופי: `wave.duration=18`, `wave.perWave=0.9`, `photog.unlock=4`, `tank.unlock=6`.

- **הפצה: GitHub Pages, לא Claude Artifact, לא קובץ יחיד** — **למה:** נוסה קודם Artifact (claude.ai/code/artifact/...) — המשתמש דחה כי זה דורש חשבון קלוד ("לא לכולם יש פרו"). אז נוסה קובץ HTML עצמאי אחד עם כל התמונות מוטמעות ב-base64 (עדיין קיים: `יהלי לוין - שלח בוואטסאפ.html`, 2.7MB) — עובד אבל דורש "פתח באמצעות" ידני בכל מכשיר. הפתרון הסופי: ריפו ציבורי בגיטהאב + GitHub Pages, כי היה למשתמש כבר חשבון גיטהאב פתוח ו-Git Credential Manager מותקן מקומי (push עבד מיד בלי הגדרה נוספת). קישור חי: **https://avishaicohen1112.github.io/yehali-stickers/**

- **PWA (manifest.json + sw.js) כדי להיפטר מסרגל הכתובת** — **למה:** אין דרך אחרת בדפדפן רגיל להסתיר את סרגל הכתובת; רק PWA מותקן ("הוסף למסך הבית") רץ ב-`display:standalone` בלי סרגל. Service worker הוסף כבונוס — מאפשר גם משחק אופליין (cache-first על כל 27 הקבצים הסטטיים).

- **אייקון האפליקציה: מתמונה אמיתית של יהלי, לא הציור** — **למה:** בקשה מפורשת של המשתמש. שלוש איטרציות: (1) אייקון מצויר (סטיקר עם פינה מקולפת) — הוחלף. (2) תמונה אמיתית + מסגרת זהב + כיתוב שטוח עם מתאר שחור — המשתמש ביקש "יותר רשמי ויפה". (3) הוסרה המסגרת, הכיתוב עבר לגרדיאנט לבן→זהב→כתום עם צל רך (**אותו טיפול בדיוק כמו `.logo-main` ב-`css/style.css`** — לא המצאה חדשה, שחזור של הסטייל הקיים במשחק). **נדחה:** להשאיר טקסט עם מתאר שחור קשה — נראה "לא רשמי" למשתמש.

- **שמות הקבצים של האייקונים שונו ל-`*-v3.png` (ולא נשארו באותו שם)** — **למה:** אחרי עדכון אייקון+שם, המשתמש הסיר והתקין מחדש את ה-PWA בטלפון (כולל ריסטרט) ועדיין ראה את הגרסה הישנה. אבחון: קובץ עם אותו שם URL עלול להיתקע מאחורי HTTP cache (הדגמנו `Cache-Control: max-age=600` על GitHub Pages) או מאחורי ה-Cache Storage של ה-service worker (לא פג תוקף לבד כמו HTTP cache). שינוי שם קובץ עוקף את שתי הבעיות בבת אחת כי זה URL חדש לגמרי. **לא מאומת סופית מול המכשיר האמיתי** — ראה שאלות פתוחות.

- **טקסט הכיתוב הוזז מעט למעלה בתוך האייקון** — **למה:** בקשת המשתמש — חשש שכיתוב קרוב מדי לקצה התחתון ייחתך על ידי מסכת העיגול (squircle) של לאנצ'ר Samsung One UI, שחותכת יותר אגרסיבית מאנדרואיד סטנדרטי.

**שינויים בפועל:**
- כל מבנה המשחק נוצר: `index.html`, `css/style.css`, `js/{utils,config,audio,input,assets,sprites,particles,entities,game}.js`, `assets/stickers/*.jpg` (10 קבצים).
- `manifest.json`, `sw.js` — נוספו לתמיכת PWA.
- `icon-192-v3.png`, `icon-512-v3.png`, `apple-touch-icon-v3.png`, `favicon-v3.png` — האייקון הסופי (תמונה אמיתית, בלי מסגרת, כיתוב גרדיאנט "יהלי והסטיקרים").
- ריפו גיטהאב `avishaicohen1112/yehali-stickers` נוצר ונדחף (5 קומיטים), GitHub Pages הופעל (branch main, root).
- `README.md` נכתב בתחילת השיחה — **כבר לא מעודכן** (ראה התיישן למטה).
- `screenshots/` — נוצרה, עם 3 צילומים כעדות לבדיקות שבוצעו.

**התיישן:**
- `README.md` — כתוב "לפתוח את index.html בדפדפן (דאבל־קליק)" כהוראת הפעלה יחידה. זה עדיין נכון טכנית אבל **לא ההמלצה** יותר: יש קישור חי (GitHub Pages) שעדיף לשליחה, ויש PWA להתקנה. גם לא מוזכר שם ב-README התקלה עם שיוך קבצי HTML במחשב של המשתמש (ל-IE) והפתרון (`פתח את המשחק.bat`).

**נשאר פתוח:**
- **הכי דחוף:** המשתמש עוד לא אישר שהאייקון v3/v4 (שם קובץ חדש + טקסט מוגבה) בפועל נראה נכון בטלפון אחרי הסרה+התקנה מחדש. זו הייתה הבקשה האחרונה ממש לפני קריאת הסקיל הזה.
- כיתוב פוגעני באחד הסטיקרים (`st_million.jpg` — "בשביל מליון שקלים היית מזיין גבר?" ב-`js/config.js`) — צוין למשתמש פעם אחת, לא נענה בפירוש (להשאיר/להוריד).
- `README.md` לא עודכן בפועל (רק סומן כמיושן חלקית באינדקס) — הסקיל הזה כותב תיעוד ליבה בלבד, לא נוגע בקוד/README.
