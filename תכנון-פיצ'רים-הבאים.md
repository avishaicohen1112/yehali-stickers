# תכנון: התקדמות, כלכלה וסטיקרים נדירים

> **מסמך עבודה בתהליך.** נכתב בשיחת תכנון לפני ביצוע — המטרה שהוא יהיה הספק המדויק כשעוברים
> לביצוע בפועל. סעיפים מסומנים **סוכם** = החלטה שהתקבלה בשיחה, **פתוח** = עדיין דורש החלטה.

## 1. מטבעות + חנות — **סוכם עקרונית**

**איך מרוויחים** (משולב, לא ערוץ יחיד): ניקוד/גל בסוף ריצה + בונוסים על ביצועים (למשל קומבו/"כמעט",
המנגנון הקיים כבר ב-`js/game.js`) + משימות יומיות/הישגים. **מאוזן ולא נדיב מדי** — לא לשבור את האיזון
האמפירי הקיים שנבדק עם בוט (ר' `00-יומן-החלטות.md` § איזון קושי).

**על מה מוציאים:** **חנות קוסמטית בלבד.** לא משפיע על קושי/כוח בשום צורה — שומר על ההוגנות שכבר
הוכחה. דוגמאות אפשריות (לא סגור, רק כיוון): סקין/צבע ליהלי, צבעי טריילים/חלקיקים, עיצובי "מסך נפסלת"
חלופיים, מסגרות לגלריית הסטיקרים.

## 1א. מבנה טכני של החנות — מוכן מראש, גם בלי פריטים בפועל

נבנה עכשיו כתשתית ריקה כדי שכשיהיו פריטים, הוספתם לא תדרוש לגעת במנוע — בדיוק כמו שהוספת סטיקר חדש
היום היא רק שורה ב-`CFG.stickers`. הכל מבוסס על תבניות שכבר קיימות ועובדות בקוד (לא המצאה חדשה):

**תצורה (`js/config.js`):** מערך חדש `CFG.shopItems = []` (ריק כרגע), באותו מבנה בדיוק כמו `CFG.stickers`
הקיים — כל item: `{id, category, name, cost}`. קטגוריות אפשריות: סקין ליהלי, צבע טריילים, עיצוב "נפסלת",
מסגרת גלריה.

**שמירה (`js/utils.js` + `js/config.js`):** אותו pattern בדיוק שכבר קיים ל-`best`/`unlocked` —
`this.best`/`this.unlocked` נטענים ב-constructor עם `U.store.get(CFG.keys.X, default)` (`js/game.js:66-67`)
ונשמרים עם `U.store.set` (שורות 272, 404). ל-2 מפתחות חדשים ב-`CFG.keys`: `coins` (מספר) ו-`shopOwned`/
`shopEquipped` (אובייקטים, בדיוק כמו `unlocked`).

**ממשק:** מסך "shop" נוסף לרשימת המסכים הקיימת (`js/game.js:121`), עם כפתור בתפריט הראשי ליד `btnGallery`
(שורה 135). `buildShop()` היא שכפול ישיר של `buildGallery()` (סביב שורה 178) — לולאה על `CFG.shopItems`,
בונה כרטיסים ב-`#shopGrid` חדש (מקביל ל-`#galleryGrid`). **מצב ריק:** אם `CFG.shopItems.length === 0`
מוצגת הודעת "בקרוב" בתוך הרשת — מצב תקין, לא באג, ולא צריך תנאי מיוחד בקוד מעבר לבדיקת אורך המערך.

**נקודות חיבור עתידיות (hooks) לכל קטגוריית קוסמטיקה** — כדי שהוספת פריט תתחבר בלי לגעת במנוע:
- סקין ליהלי → `js/sprites.js`, פונקציית ציור השחקן קוראת ל-`equipped.skin`.
- צבע טריילים/חלקיקים → `js/particles.js`.
- עיצוב "נפסלת" → `css/style.css` → `.over-stamp`, קלאס נוסף לפי המצויד.
- מסגרת גלריה → `.gcard`, קלאס נוסף.

**מטבעות בפועל:** `this.coins` נטען/נשמר באותה שיטה, מוצג ב-UI ליד `bestScore` הקיים. נקודת ההזנה
בסוף ריצה — ממש ליד המקום ש-`this.best` כבר מתעדכן (`js/game.js:271-273`).

**חשוב לסעיף 3 (לא רק לחנות):** שורות 403-404 (`this.unlocked[idx] = true; U.store.set(...)`) הן
בדיוק הנקודה שזזה כשמממשים את המנגנון-בלייב החדש — הקונטיינר `unlocked`/`buildGallery` נשאר זהה,
רק **מי קורא** לשורה הזו ומתי משתנה (מ"נפגעת" ל"ביצעת הישג").

## 2. מסלול התקדמות נפרד מהחנות — **פתוח, רעיון ראשוני בלבד**

המשתמש: "אולי נעשה מערכת התקדמות נחמדה שלא קשורה [לחנות] אבל אני לא סגור על זה."

כיוון אפשרי: משהו כמו רמת-שחקן/XP מצטבר שלא נקנה בכסף, נצבר סתם מלשחק — פותח דברים שלא קוסמטיים
בלבד (מפות? סוגי אויב? הסטיקרים הנדירים מאוד?). **טרם הוחלט:** האם זה בכלל צריך להתקיים כמסלול נפרד,
או שההתקדמות בפועל היא פשוט "מפות/אויבים/סטיקרים נדירים נפתחים ישירות ע"י הישגים" בלי רמה/XP באמצע.

## 3. מנגנון חדש לאיסוף סטיקרים לגלריה — **סוכם עקרונית, פרטים פתוחים**

**מה נשאר בדיוק כמו היום:** העונש החזותי (חבר תופס אותך → סטיקר נדבק על המסך ומסתיר שדה ראייה,
5 = "נפסלת") — **בלי שום שינוי**. זה נשאר הליבה של המשחק.

**מה משתנה:** פתיחת סטיקר **לצמיתות בגלריה** (המסך "אוסף הסטיקרים") מתנתקת מהעונש. כרגע (במשחק
הקיים) נפתח כשנפגעים ממנו לראשונה — זה משתנה לזכייה ע"י פעולה **בלייב, בתוך ריצה, ע"י ניצחון/ביצוע**,
לא ע"י הפסד.

### 3א. טקסונומיית נדירות — **סוכם ע"י המשתמש (2026-08-20), 5 דרגות**

מהנפוץ לנדיר ביותר: **רגיל → נדיר → גולש → סיישל → אולטרה סיישל**. "סיישל" הוא מונח שכבר קיים במשחק
(השדרוג הנדיר בציון — ר' `README.md`/`00-גלוסרי.md`) — שימוש חוזר מכוון, לא מקרי, לעקביות מותגית.

### 3ב. סטים (Sets) — **סוכם עקרונית, תוכן טרם נשלח**

- כל סטיקר שייך לסט נושאי (למשל "יהלי תינוק", "יהלי ברומניה"). **רשימת הסטים בפועל תישלח בהמשך** —
  המשתמש ביקש שהתשתית תהיה מוכנה קודם.
- **לכל סט יש גם רמת-נדירות משלו, שמכפילה את נדירות הפריט בתוכו** — לא תחליף לדרגה מסעיף 3א, שכבה
  נוספת עליה. דוגמה מהמשתמש: סטיקר "רגיל" שנמצא בתוך סט שהוגדר "נדיר" הוא בפועל נדיר יותר מסטיקר
  "רגיל" בסט לא-נדיר.
- **חלק מהסטים נעולים למפה ספציפית** — תלוי בפיצ'ר המפות (עדיין לא תוכנן, ר' "מה עוד נשאר לתכנן").
- **חלק מהסטים חורגים מסולם 5-הדרגות לגמרי** — נדירות מותאמת-אישית שהמשתמש יגדיר ידנית לכל סט כזה,
  לא נגזרת אוטומטית מהטקסונומיה הרגילה.

### 3ג. ערוץ איסוף שלישי: "בפוקס" — מזל טהור, לא מיומנות — **סוכם עקרונית**

בנוסף למנגנון-הבלייב מבוסס-מיומנות (למטה) — ערוץ **אקראי לגמרי**, בעיקר לדרגות הכי נדירות: סיכוי קטן
לא-תלוי-בביצוע ש"נופל" עליך פתאום תוך כדי ריצה. שני הערוצים לא מתחרים, נערמים זה על זה.

### 3ד. סטיקרי וידאו — **סוכם עקרונית**

חלק מהסטיקרים העתידיים יהיו וידאו, לא תמונה סטטית. `assets/stickers-video/` כבר קיימת ומחכה
(ר' `00-סטטוס.md`) — **אין עדיין קוד שמציג וידאו** (לא במסך הפגיעה, לא בגלריה) — ידרוש הרחבה נפרדת
של `js/assets.js`/`js/game.js` כשמגיעים לשלב הביצוע.

### 3ה. מבנה נתונים מוצע (טרם ממומש — שלד לביצוע עתידי)

הרחבת `CFG.stickers` הקיים, שדות נוספים לכל item:
```
{ file, caption, set: 'יהלי-תינוק', tier: 'נדיר', media: 'image' }   // media: 'image' | 'video'
```
+ טבלה חדשה `CFG.sets`: `{ id, name, rarityMultiplier, unlockMap: null | 'מפה-X' }` —
  `rarityMultiplier` ברירת מחדל 1, ערך מותאם-אישית לסטים החורגים מהסולם (סעיף 3ב).
+ טבלה חדשה `CFG.rarityTiers`: סדר 5 הדרגות (3א) + משקל-בסיס לכל דרגה — **אין עדיין מספרים מדויקים**,
  זה שלד לכוונון עתידי, לא החלטה סגורה.

**איך המשתמש ישלח את החומר בהמשך:** ליד כל סטיקר שיישלח (בשם הקובץ כמו שתוכנן, או סתם בטקסט ליד
התמונה בצ'אט) יצוין סט + דרגה — הנתונים ייכנסו ל-`CFG.stickers`/`CFG.sets` ידנית, אין צורך בתבנית
קשיחה אחת לשם הקובץ.

**מה בדיוק ה"מנגנון בלייב" מבוסס-המיומנות?** עדיין **הכי פתוח** מכל הסעיף. כמה כיוונים ריאליים למנוע
הקיים (Canvas 2D, גלים, אין עדיין שום "פריט אספה" במשחק):
- **פריט נדיר צף בזירה**, מופיע לעיתים רחוקות (למשל פעם ל-X גלים), צריך לאסוף אותו (לגעת בו) בלי
  להיפגע X שניות אחרי — סיכון-מול-תגמול, מתאים לקצב המשחק הקיים.
- **הישג על ביצוע** — לשרוד N גלים ברצף בלי פגיעה, קומבו מקסימלי (×8, כבר קיים), לנצח גל שלם בלי
  להיפגע מסוג אויב ספציפי.
- **מוזג עם משימות יומיות/הישגים** — אותו מנגנון תשתית שיוזן גם למטבעות (סעיף 1) יכול להזין גם
  פתיחת סטיקר נדיר, לא צריך שתי מערכות נפרדות.

## 4. מפות — **פתוח, דרישה אחת סוכמה**

**גדולות יותר** מהזירה הנוכחית — זו הדרישה הקונקרטית היחידה שנאמרה עד כה. עדיין פתוח: ויזואלי-בלבד
(רקע/פלטה שונים על אותו gameplay) מול gameplay שונה בפועל (מכשולים/גדלי-זירה שונים בין מפה למפה) —
היקף שונה לגמרי בין השתיים, לא הוכרע.

## 5. שדרוגים (power-ups) — חדשים ועדכון קיימים — **רעיונות ראשוניים, לא סגור**

### בעיית האיזון שהמשתמש רוצה לפתור
"עוד דרכים להיפתר מאויבים כי הם לא מפסיקים לעשות ספאון" — זו **מטרת עיצוב**, לא רק בקשת-תוכן: הגלים
לא נעצרים, החברה מצטברים (ר' `00-יומן-החלטות.md` § "סמן זהב... בבדיקות בוט עם 9 חברה על המסך").
שדרוג חדש שמנקה/מרחיק אויבים תורם ישירות לבעיה הזו — לא רק "עוד תוכן".

### רעיונות לשדרוגים חדשים
- **יהלי פיצה** — זורק פיצות. (השם כבר קיים כ*כינוי* במסך "נפסלת" — `README.md` — הרעיון החדש: להפוך
  אותו גם לשדרוג בפועל, שני דברים נפרדים באותו שם.)
- **"גולאש"** (שם עבודה, לא סופי) — יהלי דופק בבטן חזק, מפיל אויבים קרובים — אפקט AOE/knockback מקומי.
  שני הרעיונות האלה תורמים ישירות ל"בעיית האיזון" למעלה.

### עדכון לשדרוג קיים: דואלינגו (`js/config.js` → `power.duo`, כרגע: "כל החברה קופאים במקום")
- טקסט על המסך בהפעלה: **"רגע, אני לומד דואלינגו"** — כל החברה עוצרים ומביטים בו. (קרוב מאוד לכיתוב
  שכבר קיים על סטיקר הדואלינגו עצמו: `"ששש אני לומד דואלינגו"` ב-`js/config.js` → `stickers` — כדאי
  לשמור עקביות בין השניים כשמנסחים סופית.)
- **גרסה נדירה יותר** של אותו שדרוג — משך הקפאה ארוך יותר. **טרם הוחלט**: שדרוג נפרד לגמרי, או אותה
  יכולת בדרגת-נדירות שונה (מתחבר לטקסונומיית הנדירות מסעיף 3א — עוד שימוש אפשרי לאותו סולם).
- בזמן ההקפאה — בועות טקסט של יהלי מדבר ספרדית עם שגיאות, מילים בסיסיות מדואולינגו. **הדוגמאות
  מהמשתמש, להשתמש בהן כמו שהן, לא להמציא נוספות בלי לשאול**:
  - "אעממ... מנזנס זה מלפפון" (הבדיחה: manzanas = תפוחים, לא מלפפון — הוא טועה בכוונה)
  - "אגואה" (agua = מים)
  - "אבא" / "אמא" (papá/mamá — מילים בסיסיות של מתחילים בדואלינגו)

### כוונון קושי לשדרוגים (Nerf) — **ממצא טכני חשוב**
המשתמש: פאנישר **אמור להיות נדיר יחסית**. בדקתי את `js/config.js` — **לאויבים כבר יש שדה `weight`
לכל סוג** (`chase`/`sprint`/`photog`/`tank`, שורות ~48-69) שקובע תדירות הופעה יחסית, **אבל לשדרוגים
(`power.duo`/`boeing`/`wagyu`/`pun`) אין כרגע שום שדה כזה** — כנראה מופיעים בהסתברות שווה. נרף לפאנישר
דורש **שדה חדש** (`weight` או מקביל) שעדיין לא קיים במבנה הנתונים של השדרוגים, לא רק שינוי מספר.

## 6. אויבים חדשים — **פתוח, רק כיוון**

- עוד סוגי אויב, מעבר ל-4 הארכיטיפים הקיימים (רודף, ספרינטר-מסתער, זורק-מרחוק, טנק-כבד) — אויב חדש
  צריך התנהגות שלא חופפת לאחד מהם.
- **אויבים שונים לפי מפה** — לא כל סוגי האויב בכל מפה. מצטרף לסטים-נעולים-למפה (סעיף 3ב) כתלות שנייה
  בפיצ'ר המפות — כדאי לתכנן את שתיהן ביחד כשמגיעים למפות בפועל, לא בנפרד.

## מה עוד נשאר לתכנן

- **משימות יומיות/הישגים** — הרשימה בפועל (אילו יעדים), ואיך "יומי" נשמר בלי שרת (localStorage +
  תאריך, כמו שהשיא כבר נשמר היום).
- **פרטי מפות** (סעיף 4) — ויזואלי-בלבד מול gameplay שונה, וכל מה שתלוי בזה (סטים נעולים-למפה, אויבים
  לפי מפה).
- **מספרים מדויקים** — משקלי-נדירות לשדרוגים, לדרגות הסטיקרים, למשך הדואלינגו הנדיר — אף אחד מאלה
  עדיין לא הוגדר מספרית, רק כיוון עקרוני.
